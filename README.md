# DocForge

DocForge is an AI-powered developer documentation and code explanation tool. This repository is an initial VS Code extension scaffold written in TypeScript.

Getting started

- Install dependencies: `npm install`
- Compile: `npm run compile`
- Run in Extension Development Host: press F5 in VS Code

Command

- `DocForge: Hello` — shows a simple information message to verify the extension is loaded.
